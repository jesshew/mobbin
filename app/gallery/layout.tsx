import React from 'react';

export const metadata = {
  title: "UI Annotation Gallery | forMobbin",
  description: "Browse through your UI design batches and annotation results",
};

export default function GalleryLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 